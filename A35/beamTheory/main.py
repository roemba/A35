import numpy as np

class beamTheory:
    def staticEquilibrium(self, c_a, l_a, x_1, x_2, x_3, x_a, P, q):
        print "test"
        variableMatrix = np.matrix([[1, 0, 0, 1, 0, 0, 1, 0, ]
                                    ])